<h2>New comment</h2>

<% form_for([@post, @comment], :url => post_comments_path(@post)) do |f| %>
    <%= render :partial => 'form', :locals => {:f => f} %>
    <p><%= f.submit "Create" %> or <%= link_to 'Cancel', post_path(@post) %></p>
<% end %>


